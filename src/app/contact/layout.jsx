export const metadata = {
  title: "Contact — Jordan Montenegro",
  description: "Get in touch about research collaborations or applications.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
